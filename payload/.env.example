# Payload CMS - use same DB idea: SQLite file in project (no extra server)
# For PostgreSQL (same server as Laravel): use DATABASE_URL=postgresql://user:pass@127.0.0.1:5432/capcut_payload
DATABASE_URL=file:./payload.sqlite

# Required - set a long random string in production
PAYLOAD_SECRET=your-secret-key-change-in-production

# Optional - Payload admin URL (for CORS if frontend on different origin)
# PAYLOAD_PUBLIC_SERVER_URL=http://localhost:3001
